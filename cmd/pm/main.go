// Command pm is the Plaintext Tasks CLI: it reads, validates, queries, formats,
// and safely mutates *.tasks.yaml project files.
//
// This is a scaffold placeholder. The real command tree (cobra root, global
// flags, and error/exit-code mapping) is built in task T1 of implementation-plan.md.
package main

import "fmt"

func main() {
	fmt.Println("pm: not implemented yet")
}
